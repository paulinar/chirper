<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:background="@color/white">

    <ImageView
        android:id="@+id/ivProfilePic"
        android:background="@drawable/ic_launcher"
        android:layout_width="@dimen/my_profile_image_width"
        android:layout_height="@dimen/my_profile_image_height"
        android:layout_marginRight="@dimen/my_profile_image_margin_right"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/ivCancel"/>

    <TextView
        android:id="@+id/tvName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="@string/name"
        android:textStyle="bold"
        android:textSize="@dimen/my_profile_name_size"
        android:textColor="@color/black"
        android:layout_below="@id/ivCancel"
        android:layout_alignTop="@id/ivProfilePic"
        android:layout_toRightOf="@id/ivProfilePic"
        android:layout_centerHorizontal="true" />

    <TextView
        android:id="@+id/tvUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="@string/username"
        android:textColor="@color/gull_gray"
        android:textSize="@dimen/my_profile_name_size"
        android:layout_below="@id/tvName"
        android:layout_toRightOf="@id/ivProfilePic"
        android:layout_centerHorizontal="true" />

</RelativeLayout>